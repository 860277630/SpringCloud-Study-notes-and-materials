spring:
  config:
    use-legacy-processing: true
  profiles.active: uat
  application:
    name: seata-demo1
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        group: DEFAULT_GROUP
        username: nacos
        password: nacos
        file-extension: yaml
        refresh-enabled: true